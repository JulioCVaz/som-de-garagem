import { IOptions } from "../";
import { ITokenizer } from "strtok3";
import { Promise } from "es6-promise";
import { INativeMetadataCollector } from "../common/MetadataCollector";
import { BasicParser } from "../common/BasicParser";
/**
 * APE_HEADER: describes all of the necessary information about the APE file
 */
export interface IHeader {
    compressionLevel: number;
    formatFlags: number;
    blocksPerFrame: number;
    finalFrameBlocks: number;
    totalFrames: number;
    bitsPerSample: number;
    channel: number;
    sampleRate: number;
}
export declare class APEv2Parser extends BasicParser {
    /**
     * Calculate the media file duration
     * @param ah ApeHeader
     * @return {number} duration in seconds
     */
    static calculateDuration(ah: IHeader): number;
    static parseFooter(metadata: INativeMetadataCollector, tokenizer: ITokenizer, options: IOptions): Promise<void>;
    private static parseTags;
    private type;
    private ape;
    parse(): Promise<void>;
    private parseDescriptorExpansion;
    private parseHeader;
}
